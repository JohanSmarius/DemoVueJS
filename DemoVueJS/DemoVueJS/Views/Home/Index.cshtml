@model IEnumerable<CourseModel>


<div class="row">
    <div class="col-md-12">
        <table>
            <thead>
            <tr>
                <td>Name</td>
                <td>Start date</td>
                <td>Duration</td>
                <td>Teacher</td>
            </tr>
            <tbody>
            @foreach (var course in Model)
            {
                <tr>
                    <td>@course.Name</td>
                    <td>@($"{course.StartDate:d}")</td>
                    <td>@course.Duration</td>
                    <td>@course.Teacher</td>

                </tr>
            }
            </tbody>
        </table>
        
        <a asp-controller="Home"
           asp-action="AddCourse">Add Course</a>
    </div>
</div>
